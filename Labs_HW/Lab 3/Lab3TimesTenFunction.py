# The purpose of this program is to accept an input and display the product of its inpuit multiplied by 10.
# Creating the function to do the calculation.
def times_ten(number):
    result = number * 10
    print(result)

# Assiging the user input to a the "user_input" variable as a flloat to enable calculation.    
user_input = float(input("Enter a number: "))

times_ten(user_input) 